/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  images: {
    domains: ['www.bonzicart.com', 'admin.bonzicart.com', "via.placeholder.com"],
  },
};

export default nextConfig;
